// var inputContainer = document.querySelectorAll('.input-custom');
// var input = document.querySelectorAll('.input__field--hoshi');

// var inputContainer = document.querySelector('.input-custom');
// var input = document.querySelector('.input__field--hoshi');
//
// input.addEventListener('blur', function() {
//     input.value === "" ? inputContainer.classList.remove('input--filled') : inputContainer.classList.add('input--filled');
// });
//
// input.addEventListener('!focus', function() {
//     input.value === "" ? inputContainer.classList.remove('input--filled') : inputContainer.classList.add('input--filled');
// });

/*for (i = 0; i < inputContainer.length; i++) {

    for (ii = 0; ii < input.length; ii++) {
        var inputContainer = inputContainer[i];
        var input = input[ii];

        console.log(inputContainer)
        console.log(input)

        input.addEventListener('blur', function () {
            input.value === "" ? inputContainer.classList.remove('input--filled') : inputContainer.classList.add('input--filled');
        });

        input.addEventListener('!focus', function () {
            input.value === "" ? inputContainer.classList.remove('input--filled') : inputContainer.classList.add('input--filled');
        });
    }
}*/

// for (i = 0; i < inputContainer.length; i++) {
//     console.log(inputContainer[i])
//     for (ii = 0; i < input.length; ii++) {
//         console.log(input[ii])
//     }
// }





